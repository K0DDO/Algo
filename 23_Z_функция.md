# 23. Z-функция

## Определение

Z-функция строки `S` - это массив `z`, где `z[i]` равно длине наибольшего префикса всей строки `S`, совпадающего с подстрокой, начинающейся в позиции `i`.

Формально:

```text
z[i] = max L: S[0..L-1] = S[i..i+L-1]
```

По соглашению из лекции:

```text
z[0] = 0
```

Хотя вся строка совпадает сама с собой, в алгоритмах обычно удобнее считать `z[0]` нулем.

## Пример `domodedovo`

В лекции разбирается строка:

```text
S = domodedovo
```

Ее Z-массив:

```text
S: d o m o d e d o v o
z: 0 0 0 0 1 0 2 0 0 0
```

Пояснение:

- `z[4] = 1`, потому что подстрока с позиции 4 начинается с `d`, как и вся строка, но следующий символ уже не совпадает.
- `z[6] = 2`, потому что с позиции 6 стоит `do`, совпадающее с префиксом `do`.
- остальные позиции не начинаются с `d` или быстро расходятся.

## Наивный алгоритм

Для каждой позиции `i` можно сравнивать символы `S[z[i]]` и `S[i + z[i]]`, пока они равны.

```text
compute_z_naive(S):
    z = array(len(S), 0)
    for i = 1..len(S)-1:
        while i + z[i] < len(S) and S[z[i]] == S[i + z[i]]:
            z[i] += 1
    return z
```

Сложность:

```text
O(n^2)
```

На строках с большим количеством одинаковых символов, например `aaaaaaaa`, каждая позиция может сравниваться долго.

## Z-блок

Оптимальный алгоритм хранит текущий `z-блок`.

Z-блок - это отрезок `[L, R]`, который совпадает с префиксом строки:

```text
S[L..R] = S[0..R-L]
```

Алгоритм идет слева направо и поддерживает z-блок с максимальным правым концом `R`.

Идея:

- если `i > R`, позиция не покрыта известным блоком, считаем `z[i]` простым сравнением;
- если `i <= R`, позиция находится внутри блока, значит часть ответа можно взять из уже посчитанного `z[i - L]`;
- если известного значения хватает внутри `[L, R]`, просто копируем;
- если значение выходит за `R`, начинаем с границы `R` и досчитываем дальше.

## Линейный алгоритм

```text
compute_z(S):
    n = len(S)
    z = array(n, 0)
    L = 0
    R = 0

    for i = 1..n-1:
        if i <= R:
            z[i] = min(R - i + 1, z[i - L])

        while i + z[i] < n and S[z[i]] == S[i + z[i]]:
            z[i] += 1

        if i + z[i] - 1 > R:
            L = i
            R = i + z[i] - 1

    return z
```

Это компактная версия правил из лекции:

- поддерживаем `[L, R]` с максимальным `R`;
- если `i > R`, считаем циклом;
- если `i + z[i-L] <= R`, то `z[i] = z[i-L]`;
- если `i + z[i-L] > R`, то `z[i] = R - i + 1` и пробуем увеличить циклом;
- после досчета обновляем `[L, R]`.

## Почему сложность `O(n)`

Внутренний `while` действительно может выполняться много раз на отдельных позициях. Но каждый успешный шаг `while` увеличивает правую границу `R`.

Так как `R` не может увеличиться больше чем до `n-1`, суммарное число успешных сравнений за весь алгоритм `O(n)`. Внешний цикл тоже `O(n)`.

Итого:

```text
Время: O(n)
Память: O(n)
```

## Поиск подстроки через Z-функцию

Чтобы найти образец `P` в тексте `T`, строим строку:

```text
S = P + "#" + T
```

Символ `#` не должен встречаться ни в `P`, ни в `T`.

Считаем `z` для `S`. Если:

```text
z[i] = |P|
```

то в тексте найдено вхождение, начинающееся в позиции:

```text
i - |P| - 1
```

Сложность:

```text
O(|P| + |T|)
```

Память:

```text
O(|P| + |T|)
```

В лекции отмечено, что можно не хранить `z` для части текста, но потоковая обработка с Z-функцией менее удобна, чем с префикс-функцией.

## Сравнение с префикс-функцией

Z-функция и префикс-функция решают похожие задачи, но смотрят на строку с разных сторон:

- `z[i]` говорит, сколько символов от позиции `i` совпадает с началом всей строки;
- `pi[i]` говорит, какой максимальный префикс является суффиксом строки `S[0..i]`.

Для поиска подстроки обе дают `O(|P| + |T|)`.

Z-функция часто проще для задач вида "найти совпадения префикса со всеми позициями". Префикс-функция удобнее для потокового КМП и построения автомата.

## Структуры данных

- Строка `S`.
- Массив `z`.
- Две границы `L`, `R`.
- Для поиска подстроки - объединенная строка `P#T` и массив ответов.

## Ограничения

- Нужен разделитель, отсутствующий в алфавите данных.
- Алгоритм ищет точные совпадения.
- Для очень больших текстов хранение `P#T` может быть неудобным.
- Если требуется потоковая обработка, чаще выбирают КМП.

## Применимость

- Поиск всех вхождений образца.
- Поиск периодов строки.
- Сравнение префикса со всеми суффиксами.
- Задачи на строковые повторы и границы.
- Быстрое построение вспомогательной информации для строковых алгоритмов.

## Экзамен-вопросы

1. Дайте определение Z-функции.
2. Почему обычно принимают `z[0] = 0`?
3. Посчитайте Z-функцию для `domodedovo`.
4. Что такое z-блок `[L, R]`?
5. Почему наивный алгоритм имеет сложность `O(n^2)`?
6. Почему оптимальный алгоритм работает за `O(n)`?
7. Как с помощью Z-функции найти все вхождения `P` в `T`?
