# Код Хемминга

## Введение / определения с нуля

Код Хемминга — это способ добавить к сообщению контрольные биты так, чтобы при передаче можно было обнаружить и исправить одну ошибку в одном бите.

Задача возникает из-за того, что при хранении или передаче данных бит может исказиться:

```text
было:  1011001
стало: 1010001
             ^
```

Один бит изменился, и получатель хочет понять:

- есть ли ошибка;
- где именно ошибка;
- как восстановить исходное сообщение.

Код Хемминга решает задачу исправления одной ошибки за счет избыточности.

Основные термины:

- информационные биты — исходные биты сообщения;
- проверочные биты — дополнительные биты, которые вычисляются по информационным;
- кодовое слово — вся последовательность после добавления проверочных битов;
- синдром — число, вычисленное при проверке принятого слова; оно показывает позицию ошибочного бита.

Классический двоичный код Хемминга исправляет одну ошибку.

Если дополнительно добавить общий бит четности, можно получить расширенный код Хемминга, который:

- исправляет одну ошибку;
- обнаруживает две ошибки;
- обычно обозначается как SECDED: Single Error Correction, Double Error Detection.

Дальше сначала рассматривается обычный код Хемминга без общего бита четности.

Позиции битов нумеруются с 1, не с 0:

```text
позиции: 1 2 3 4 5 6 7 8 ...
```

Проверочные биты ставятся в позиции, являющиеся степенями двойки:

```text
1, 2, 4, 8, 16, ...
```

Остальные позиции занимают информационные биты.

Например, для 4 информационных битов нужно 3 проверочных:

```text
позиция:      1  2  3  4  5  6  7
тип:          p1 p2 d1 p4 d2 d3 d4
```

Здесь:

- `p1`, `p2`, `p4` — проверочные биты;
- `d1`, `d2`, `d3`, `d4` — данные.

Почему именно степени двойки? Потому что номер каждой позиции можно представить в двоичном виде. Проверочный бит `p_{2^k}` отвечает за те позиции, где в номере позиции установлен соответствующий бит.

Например, позиции от 1 до 7:

| Позиция | Двоично | Участвует в проверках |
|---:|---:|---|
| 1 | 001 | p1 |
| 2 | 010 | p2 |
| 3 | 011 | p1, p2 |
| 4 | 100 | p4 |
| 5 | 101 | p1, p4 |
| 6 | 110 | p2, p4 |
| 7 | 111 | p1, p2, p4 |

Если ошибся бит в позиции 6, то нарушатся проверки `p2` и `p4`, а `p1` не нарушится. Двоичное число `110` равно 6. Так синдром указывает позицию ошибки.

## Логика работы (пошагово + псевдокод)

### Сколько нужно проверочных битов

Пусть:

- `m` — число информационных битов;
- `r` — число проверочных битов;
- `n = m + r` — длина кодового слова.

Нужно различать:

- отсутствие ошибки;
- ошибку в любой из `n` позиций.

Всего вариантов:

$$
n + 1 = m + r + 1
$$

С помощью `r` проверочных битов можно получить:

$$
2^r
$$

разных синдромов.

Поэтому нужно:

$$
2^r \ge m + r + 1
$$

Для `m=4`:

```text
r=2: 2^2 = 4 < 4+2+1 = 7
r=3: 2^3 = 8 >= 4+3+1 = 8
```

Значит, нужно 3 проверочных бита.

### Кодирование

Пошагово:

1. Выбрать минимальное `r`, для которого `2^r >= m + r + 1`.
2. Создать массив длины `n = m + r`, нумеруя позиции с 1.
3. В позиции `1, 2, 4, 8, ...` временно поставить нули или пустые значения.
4. В остальные позиции записать информационные биты.
5. Для каждого проверочного бита вычислить четность своей группы.
6. Записать проверочные биты.

Обычно используют четность по модулю 2:

$$
a \oplus b \oplus c
$$

где `xor` равен 1, если число единиц нечетно.

При четной проверке каждый контрольный набор должен иметь четное число единиц.

Псевдокод вычисления числа проверочных битов:

```text
FindR(m):
    r = 0
    while 2^r < m + r + 1:
        r = r + 1
    return r
```

Псевдокод кодирования:

```text
EncodeHamming(data[1..m]):
    r = FindR(m)
    n = m + r
    code[1..n] = 0

    j = 1
    for i = 1..n:
        if i is power of two:
            code[i] = 0
        else:
            code[i] = data[j]
            j = j + 1

    for k = 0..r-1:
        p = 2^k
        parity = 0

        for i = 1..n:
            if (i & p) != 0:
                parity = parity xor code[i]

        code[p] = parity

    return code
```

В этом варианте проверочный бит входит в собственную проверку. Так удобно: после установки `code[p] = parity` вся группа имеет четную сумму.

### Декодирование и исправление

Пошагово:

1. Получить кодовое слово длины `n`.
2. Для каждого проверочного бита заново посчитать четность его группы.
3. Если проверка нарушена, добавить соответствующую степень двойки к синдрому.
4. Если синдром равен 0, ошибки нет.
5. Если синдром не равен 0, ошибка в позиции `syndrome`.
6. Инвертировать бит в этой позиции.
7. Вытащить информационные биты из непроверочных позиций.

Псевдокод:

```text
DecodeHamming(received[1..n]):
    r = number of powers of two <= n
    syndrome = 0

    for k = 0..r-1:
        p = 2^k
        parity = 0

        for i = 1..n:
            if (i & p) != 0:
                parity = parity xor received[i]

        if parity != 0:
            syndrome = syndrome + p

    if syndrome != 0:
        received[syndrome] = 1 - received[syndrome]

    data = empty array
    for i = 1..n:
        if i is not power of two:
            data.push(received[i])

    return data, syndrome
```

Если `syndrome = 5`, значит ошибка в пятой позиции. Если `syndrome = 0`, значит все проверки сошлись.

## Структура данных

Обычно достаточно массивов.

Для кодирования:

- `data[1..m]` — исходные информационные биты;
- `code[1..n]` — кодовое слово;
- переменная `r` — число проверочных битов;
- переменная `parity` — текущая четность.

Для декодирования:

- `received[1..n]` — принятая последовательность;
- `syndrome` — номер позиции с ошибкой;
- `corrected[1..n]` — исправленная последовательность, если не хотим менять входной массив.

Ключевая операция:

```text
(i & p) != 0
```

Она проверяет, входит ли позиция `i` в контрольную группу проверочного бита `p`.

Например:

```text
i = 6 = 110₂
p = 2 = 010₂
i & p = 010₂ != 0
```

Значит, позиция 6 участвует в проверке `p2`.

Позиция 6 также участвует в проверке `p4`, потому что:

```text
6 = 110₂
4 = 100₂
6 & 4 != 0
```

Но не участвует в проверке `p1`:

```text
6 = 110₂
1 = 001₂
6 & 1 = 0
```

## Сложность по времени и памяти

Пусть:

- `m` — число информационных битов;
- `r` — число проверочных битов;
- `n = m + r`.

Число проверочных битов растет логарифмически:

$$
r = O(\log n)
$$

Кодирование:

- размещение битов — `O(n)`;
- вычисление всех проверок — `O(rn)`.

Итого:

$$
O(n \log n)
$$

Если использовать более специализированные способы подсчета, можно оптимизировать константы, но базовая учебная реализация именно такая.

Декодирование:

- пересчет проверок — `O(rn)`;
- исправление одного бита — `O(1)`;
- извлечение данных — `O(n)`.

Итого:

$$
O(n \log n)
$$

Память:

$$
O(n)
$$

потому что храним кодовое слово и, возможно, исправленную копию.

## Ограничения

Обычный код Хемминга исправляет только одну ошибку.

Если в кодовом слове произошли две ошибки, обычный синдром может указать на неправильную позицию. Тогда декодер может попытаться исправить бит, который не был ошибочным, и испортить сообщение еще сильнее.

Обычный код Хемминга:

- обнаруживает многие ошибки;
- гарантированно исправляет одну ошибку;
- не гарантирует корректную обработку двух и более ошибок.

Для обнаружения двух ошибок используют расширенный код Хемминга с дополнительным общим битом четности.

Ограничения:

- работает с бинарными данными;
- требует добавлять избыточные биты;
- не подходит для длинных пакетов с высокой вероятностью нескольких ошибок без дополнительных механизмов;
- не заменяет криптографическую целостность;
- не защищает от намеренной подмены.

Важно: код Хемминга исправляет случайные одиночные ошибки, но не предназначен для сжатия. Он, наоборот, увеличивает длину сообщения.

## Применимость

Код Хемминга применяют:

- в учебных курсах по теории кодирования;
- в памяти и каналах связи с одиночными ошибками;
- как базовый пример корректирующих кодов;
- в задачах, где вероятность двух ошибок в одном блоке мала;
- для понимания синдромного декодирования.

Подход хорошо подходит, если:

- данные передаются блоками;
- вероятность ошибки невелика;
- нужно быстро исправлять одиночные ошибки;
- допустимо добавить несколько контрольных битов.

Не лучший выбор, если:

- ошибки часто идут пачками;
- нужен сильный контроль целостности;
- канал очень шумный;
- нужно исправлять много ошибок в одном блоке.

Для пачек ошибок используют другие коды и техники, например коды Рида-Соломона, сверточные коды, LDPC или перемежение.

## Примеры с разбором

Закодируем данные:

```text
data = 1011
```

Здесь `m = 4`.

Находим `r`:

```text
r = 3, потому что 2^3 = 8 >= 4 + 3 + 1 = 8
```

Длина кодового слова:

```text
n = 4 + 3 = 7
```

Расставляем позиции:

```text
позиция: 1  2  3  4  5  6  7
тип:     p1 p2 d1 p4 d2 d3 d4
```

Записываем данные `1 0 1 1` в непроверочные позиции:

```text
позиция: 1 2 3 4 5 6 7
значение: ? ? 1 ? 0 1 1
```

Вычисляем `p1`. Он проверяет позиции, где младший бит номера равен 1:

```text
1, 3, 5, 7
```

Значения:

```text
p1, 1, 0, 1
```

Чтобы сумма была четной:

```text
1 xor 0 xor 1 = 0
```

Значит:

```text
p1 = 0
```

Вычисляем `p2`. Он проверяет позиции:

```text
2, 3, 6, 7
```

Значения:

```text
p2, 1, 1, 1
```

```text
1 xor 1 xor 1 = 1
```

Чтобы вся группа имела четность 0, нужно:

```text
p2 = 1
```

Вычисляем `p4`. Он проверяет позиции:

```text
4, 5, 6, 7
```

Значения:

```text
p4, 0, 1, 1
```

```text
0 xor 1 xor 1 = 0
```

Значит:

```text
p4 = 0
```

Итоговое кодовое слово:

```text
позиция:  1 2 3 4 5 6 7
значение: 0 1 1 0 0 1 1
```

То есть:

```text
0110011
```

Теперь допустим, при передаче ошибка возникла в позиции 6:

```text
было:     0 1 1 0 0 1 1
получили: 0 1 1 0 0 0 1
                         ^
```

Считаем проверки.

Проверка `p1`: позиции `1, 3, 5, 7`:

```text
0 xor 1 xor 0 xor 1 = 0
```

Не нарушена.

Проверка `p2`: позиции `2, 3, 6, 7`:

```text
1 xor 1 xor 0 xor 1 = 1
```

Нарушена, добавляем 2.

Проверка `p4`: позиции `4, 5, 6, 7`:

```text
0 xor 0 xor 0 xor 1 = 1
```

Нарушена, добавляем 4.

Синдром:

```text
2 + 4 = 6
```

Ошибка в позиции 6. Инвертируем ее:

```text
0 1 1 0 0 1 1
```

Извлекаем информационные позиции `3, 5, 6, 7`:

```text
1 0 1 1
```

Получили исходное сообщение.

## Типичные ошибки / вопросы на экзамен

Типичные ошибки:

- нумеровать позиции с 0 вместо 1;
- ставить проверочные биты не в степени двойки;
- забывать, что синдром — это номер позиции ошибки;
- неправильно выбирать `r`;
- считать проверку только по информационным битам и забывать про сам проверочный бит при декодировании;
- путать четную и нечетную проверку;
- думать, что обычный Хемминг исправляет две ошибки;
- извлекать данные вместе с проверочными битами;
- складывать проверки как десятичные флаги без понимания двоичного смысла.

Вопросы на экзамен:

1. Зачем нужны проверочные биты?
2. Почему они стоят в позициях `1, 2, 4, 8, ...`?
3. Как определить число проверочных битов?
4. Что такое синдром?
5. Почему синдром указывает позицию ошибки?
6. Что будет при двух ошибках?
7. Чем обычный код Хемминга отличается от расширенного?
8. Как извлечь исходные данные после исправления?
9. Почему позиции нумеруются с 1?
10. Как построить контрольные группы?

Короткие ответы:

- Проверочные биты кодируют информацию о позиции возможной ошибки.
- Степени двойки соответствуют отдельным битам двоичного номера позиции.
- Нужно минимальное `r`, такое что `2^r >= m + r + 1`.
- Синдром — набор нарушенных проверок, интерпретируемый как двоичный номер позиции.
- При двух ошибках обычный код может дать неверное исправление.

## Дополнительно (доказательства идей, варианты)

### Почему синдром равен позиции ошибки

Рассмотрим позицию `i`. Ее двоичная запись показывает, в каких проверках она участвует.

Например:

```text
i = 5 = 101₂
```

Позиция 5 участвует в проверках:

- `p1`, потому что младший бит равен 1;
- `p4`, потому что бит четверки равен 1;
- не участвует в `p2`, потому что бит двойки равен 0.

Если именно этот бит изменился, то нарушатся ровно проверки `p1` и `p4`.

Синдром будет:

```text
1 + 4 = 5
```

То есть синдром восстановил номер ошибочной позиции.

### Матричная запись

Код Хемминга можно описать через проверочную матрицу `H`.

Столбец матрицы для позиции `i` — это двоичная запись числа `i`.

Для кода длины 7 проверочная матрица может иметь столбцы:

```text
1: 001
2: 010
3: 011
4: 100
5: 101
6: 110
7: 111
```

Синдром:

$$
s = H \cdot y^T
$$

где `y` — принятое слово.

Если ошибки нет, синдром нулевой. Если ошибка в позиции `i`, синдром равен `i`-му столбцу, то есть двоичной записи позиции.

### Расширенный код Хемминга

Добавим общий бит четности `p0`, который проверяет четность всего слова.

Тогда:

- синдром `0`, общая четность верна — ошибки нет;
- синдром не `0`, общая четность нарушена — одна ошибка в позиции синдрома;
- синдром `0`, общая четность нарушена — ошибка в общем бите четности;
- синдром не `0`, общая четность верна — вероятно две ошибки, исправлять нельзя.

Такой код обнаруживает две ошибки и исправляет одну.

### Расстояние Хемминга

Расстояние Хемминга между двумя словами — число позиций, в которых они различаются.

Например:

```text
101100
100110
  ^ ^^
```

Расстояние равно 3.

Обычный код Хемминга имеет минимальное расстояние 3. Это позволяет исправлять одну ошибку, потому что шар радиуса 1 вокруг каждого кодового слова не пересекается с шаром радиуса 1 вокруг другого кодового слова.

Расширенный код Хемминга имеет минимальное расстояние 4. Поэтому он может обнаруживать две ошибки.
