# A2. Суффиксный массив

## Задача

Для строки `S` длины `n` нужно эффективно работать с ее суффиксами:

```text
S[i..n-1]
```

Суффиксный массив - это массив индексов начал всех суффиксов строки, отсортированных в лексикографическом порядке.

Если:

```text
S = banana
```

то суффиксы:

```text
0: banana
1: anana
2: nana
3: ana
4: na
5: a
```

В отсортированном порядке:

```text
5: a
3: ana
1: anana
0: banana
4: na
2: nana
```

Суффиксный массив:

```text
SA = [5, 3, 1, 0, 4, 2]
```

## Зачем нужен суффиксный массив

Он позволяет свести многие задачи на подстроки к задачам на отсортированный массив суффиксов:

- поиск подстроки;
- количество различных подстрок;
- самая длинная повторяющаяся подстрока;
- сравнение суффиксов;
- поиск общих подстрок двух строк;
- сжатое индексирование текста.

Суффиксный массив обычно проще и компактнее суффиксного дерева.

## Поиск подстроки

Подстрока `P` входит в `S`, если среди суффиксов `S` есть суффикс, начинающийся с `P`.

Так как суффиксы отсортированы, можно сделать бинарный поиск по `SA`.

На каждом шаге сравниваем `P` с префиксом суффикса:

```text
S[SA[mid]..]
```

Сложность простого поиска:

```text
O(|P| log n)
```

Если нужен диапазон всех вхождений, ищут левую и правую границу суффиксов, начинающихся с `P`.

## LCP-массив

`LCP` - массив длин наибольших общих префиксов соседних суффиксов в суффиксном массиве.

```text
LCP[i] = lcp(S[SA[i]..], S[SA[i-1]..])
```

Для `banana`:

```text
SA:
5: a
3: ana
1: anana
0: banana
4: na
2: nana

LCP:
0, 1, 3, 0, 0, 2
```

Пояснение:

- `lcp("ana", "a") = 1`;
- `lcp("anana", "ana") = 3`;
- `lcp("nana", "na") = 2`.

## Алгоритм Касаи для LCP

После построения `SA` можно построить `LCP` за `O(n)`.

Идея:

1. Построить обратный массив `rank[pos]`: позиция суффикса `pos` в `SA`.
2. Идти по суффиксам в порядке исходных позиций.
3. Если для `S[i..]` известен LCP с предыдущим суффиксом в `SA`, то для `S[i+1..]` значение уменьшится минимум на 1.

Псевдокод:

```text
kasai(S, SA):
    n = len(S)
    rank = array(n)
    for i = 0..n-1:
        rank[SA[i]] = i

    lcp = array(n, 0)
    k = 0

    for i = 0..n-1:
        if rank[i] == 0:
            k = 0
            continue

        j = SA[rank[i] - 1]
        while i+k < n and j+k < n and S[i+k] == S[j+k]:
            k += 1

        lcp[rank[i]] = k
        if k > 0:
            k -= 1

    return lcp
```

Сложность:

```text
O(n)
```

## Построение суффиксного массива: удвоение

Классический алгоритм строит `SA` за `O(n log n)`.

Идея:

1. Сначала сортируем суффиксы по первым символам.
2. Затем по первым `2` символам.
3. Затем по первым `4`, `8`, `16` и так далее.

На шаге `k` у каждого суффикса есть класс эквивалентности для префикса длины `2^k`.

Чтобы отсортировать по `2^(k+1)`, сравниваем пару классов:

```text
(class[i], class[i + 2^k])
```

После сортировки обновляем классы.

Обычно к строке добавляют уникальный минимальный терминатор:

```text
S = S + "$"
```

Терминатор меньше всех символов и встречается один раз. Он делает все суффиксы различимыми и упрощает циклические сдвиги.

## Пример идеи на `banana$`

Суффиксы:

```text
0 banana$
1 anana$
2 nana$
3 ana$
4 na$
5 a$
6 $
```

После сортировки:

```text
6 $
5 a$
3 ana$
1 anana$
0 banana$
4 na$
2 nana$
```

Если использовать терминатор, суффиксный массив для исходной строки часто записывают без позиции терминатора:

```text
[5, 3, 1, 0, 4, 2]
```

## Количество различных подстрок

Всего подстрок строки длины `n`:

```text
n * (n + 1) / 2
```

Каждый суффикс `S[SA[i]..]` добавляет свои префиксы как подстроки. Но первые `LCP[i]` префиксов уже встречались в предыдущем суффиксе.

Количество различных подстрок:

```text
n * (n + 1) / 2 - sum LCP[i]
```

Для `banana`:

```text
total = 6 * 7 / 2 = 21
sum LCP = 0 + 1 + 3 + 0 + 0 + 2 = 6
distinct = 15
```

## Самая длинная повторяющаяся подстрока

Повторяющаяся подстрока является общим префиксом хотя бы двух суффиксов. В отсортированном порядке максимальный общий префикс обязательно встретится у соседних суффиксов.

Ответ:

```text
max LCP[i]
```

Для `banana`:

```text
max LCP = 3
подстрока = "ana"
```

## Поиск общей подстроки двух строк

Для строк `A` и `B` строят:

```text
S = A + "#" + B + "$"
```

где разделители уникальны.

Строят `SA` и `LCP`. Самая длинная общая подстрока - максимальный `LCP[i]` между соседними суффиксами, один из которых начинается в `A`, а другой в `B`.

## Сложность

Хранение:

```text
SA: O(n)
rank: O(n)
LCP: O(n)
```

Построение:

- простая сортировка суффиксов строками: до `O(n^2 log n)`;
- алгоритм удвоения: `O(n log n)`;
- продвинутые алгоритмы: `O(n)`, но сложнее в реализации.

Поиск подстроки:

```text
O(|P| log n)
```

С дополнительными структурами по LCP можно ускорять сравнения, но базовый вариант обычно достаточен для учебных задач.

## Структуры данных

- `SA` - позиции суффиксов в отсортированном порядке.
- `rank` - обратная позиция суффикса.
- `classes` - классы эквивалентности при построении.
- `LCP` - общие префиксы соседних суффиксов.
- Терминатор `$`.

## Ограничения

- Суффиксный массив статичен: при изменении строки его обычно перестраивают.
- Нужна аккуратная работа с разделителями.
- Простое сравнение суффиксов в бинарном поиске может давать лишний множитель `|P|`.
- Для задач с динамическими строками лучше подходят другие структуры.

## Применимость

- Полнотекстовый поиск.
- Биоинформатика.
- Сжатие данных.
- Поиск повторов.
- Поиск количества различных подстрок.
- Сравнение больших текстов.

## Экзамен-вопросы

1. Что такое суффиксный массив?
2. Постройте `SA` для `banana`.
3. Что такое `LCP`?
4. Как искать подстроку через суффиксный массив?
5. Почему количество различных подстрок равно `n(n+1)/2 - sum LCP`?
6. Как найти самую длинную повторяющуюся подстроку?
7. Зачем добавляют терминатор `$`?
8. В чем идея алгоритма удвоения?
9. Какая сложность построения `SA` и `LCP`?
