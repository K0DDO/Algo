***Идея арифметического кодирования***

Сообщение → один числовой интервал [L,R) ⊆ [0,1), сужаемый по символам согласно вероятностям.

Чем отличается от Хаффмана?

Может давать дробное число бит на символ; ближе к энтропии для целых сообщений.

Декодирование

Смотрим, в какой подинтервал попало число, выдаём символ, сужаем — повторяем.

Арифметическое кодирование кодирует сообщение как [...] по вероятностям символов.

Арифметическое кодирование кодирует сообщение как вещественный интервал по вероятностям символов.

***Идея кода Хаффмана***

Строим дерево по частотам: чаще → короче код; префиксный код.

Почему префиксный?

Никакой код не префикс другого ⇒ однозначное декодирование без разделителей.

Как строят дерево?

Жадно: два наименьших веса → новый узел с суммой; в куче.

Сложность построения

O(n log n) с кучей, n — размер алфавита.

Оптимальность

Среди префиксных кодов для заданных частот Хаффман оптимален по средней длине.

Хаффман даёт [...] код, оптимальный для заданных частот.

Хаффман даёт префиксный код, оптимальный для заданных частот.

Хаффман даёт префиксный код, оптимальный для заданных [...].

Хаффман даёт префиксный код, оптимальный для заданных частот.

***Для чего код Хемминга?***

Обнаружение и исправление одиночных ошибок (классический (7,4) и обобщения).

Где стоят биты чётности?

На позициях степеней двойки: 1,2,4,8,...

Что такое синдром?

Результат проверок чётности; указывает позицию ошибочного бита (если одна ошибка).

Ограничение

Классическая схема исправляет 1 ошибку; две ошибки могут быть некорректно «исправлены».

В коде Хемминга проверочные биты стоят на позициях [...].

В коде Хемминга проверочные биты стоят на позициях степеней двойки.

***Что такое DFS?***

Обход графа в глубину: идём по ребру в непосещённую вершину как можно глубже, затем откатываемся (рекурсия/стек).

Какие три цвета вершин используют в DFS (лекция)?

white — не посещали; gray — вошли, ещё не вышли (в стеке); black — полностью обработали.

Сложность DFS по времени и памяти (список смежности)?

Время O(V+E), память O(V) на цвета/стек (+ O(V+E) на граф).

На какой структуре данных реализуют DFS?

Список смежности + массив цветов (или visited) + рекурсия / явный стек.

Как DFS находит цикл в ориентированном графе?

Если есть ребро в серую (gray) вершину — это обратное ребро ⇒ цикл.

Почему в неориентированном графе ребро к родителю — не цикл?

Это то же ребро дерева DFS в обратную сторону; цикл — ребро к visited-вершине, которая не родитель.

Что означает переход DFS в чёрную вершину в орграфе?

Вершина уже полностью обработана: forward или cross edge (не обязательно цикл).

Ограничения/особенности DFS

Не даёт кратчайших путей по числу рёбер; глубина рекурсии до O(V); порядок зависит от порядка соседей.

Применимость DFS

Компоненты связности, циклы, топосорт, SCC/конденсация, мосты/точки сочленения, раскраска/двудольность.

Лемма о DFS: бывает ли ребро из чёрной в белую?

Нет: в процессе DFS не существует ребра из чёрной вершины в белую.

Сформулируй лемму о белых путях (идея)

Пока серая u обрабатывается до black: бывшие gray/black не меняются; белые либо остаются белыми, либо чернеют — те, что достижимы от u по белым путям.

В DFS при входе вершина красится в [...], при выходе — в black.

В DFS при входе вершина красится в gray, при выходе — в black.

В DFS при входе вершина красится в gray, при выходе — в [...].

В DFS при входе вершина красится в gray, при выходе — в black.

***Что такое BFS?***

Обход в ширину: вершины обрабатываются слоями по удалению от старта; очередь FIFO.

Сложность BFS?

O(V+E) время, O(V) доп. память (очередь + массивы).

Какие массивы обычно держат вместе с BFS?

d[] — расстояние (число рёбер); parent[] — для восстановления пути; очередь «горящих» вершин.

Что такое дерево BFS?

Дерево, где ребро в непосещённую вершину добавляется при постановке в очередь; путь в дереве = кратчайший по числу рёбер от s.

Почему BFS даёт кратчайшие пути в невзвешенном графе?

Вершины извлекаются в порядке неубывания расстояния; впервые достигнутая вершина достигнута минимальным числом рёбер.

Ограничения BFS

Для взвешенных графов некорректен как кратчайшие пути (нужны Дейкстра/BF и т.д.).

Применимость BFS

Расстояния/пути в невзвешенном графе, компоненты, кратчайшие увеличивающие пути (Эдмондс–Карп), двудольность.

BFS использует структуру [...] и даёт кратчайшие пути по числу рёбер.

BFS использует структуру очередь (FIFO) и даёт кратчайшие пути по числу рёбер.

BFS использует структуру очередь (FIFO) и даёт кратчайшие пути по [...].

BFS использует структуру очередь (FIFO) и даёт кратчайшие пути по числу рёбер.

***Что такое топологическая сортировка?***

Упорядочивание вершин орграфа так, что для любого ребра (u,v) вершина u идёт раньше v.

На каких графах существует топосорт?

Только на ациклических ориентированных (DAG).

Как получить топосорт через DFS?

DFS; при выходе (black) кладём вершину в список/стек; ответ — разворот списка времён выхода.

Что такое алгоритм Демукрона (идея)?

Итеративно брать вершины с входящей степенью 0, удалять их и рёбра, повторять (как Кана / уровни).

Сложность топосорта?

O(V+E).

Как топосорт связан с гамильтоновым путём?

В лекции: с помощью топосорта можно искать гамильтонов путь (в подходящих орграфах/условиях).

Применимость топосорта

Порядок задач с зависимостями, компиляция, кратчайшие/длиннейшие пути в DAG.

Топосорт существует ⟺ орграф [...].

Топосорт существует ⟺ орграф ациклический (DAG).

***Что такое компонента сильной связности (SCC)?***

Максимальное множество вершин орграфа, где из любой в любую есть направленный путь (туда и обратно).

Что такое слабая связность орграфа?

Связность неориентированной версии: забываем направления рёбер.

Как найти компоненты слабой связности?

Построить неориентированную копию графа и запустить DFS/BFS с пометкой компонент.

Алгоритм Косарайю (3 этапа из лекции)

1) Обратный граф H (или сначала DFS на G). 2) DFS и времена выхода f[u]. 3) DFS по вершинам в порядке убывания f[u] на другом графе; деревья = SCC.

Псевдокод Косарайю (стек)

dfsG кладёт вершины в стек при выходе; затем пока стек не пуст: если component[u]==0 → dfsH, count++.

Что такое конденсация графа?

Граф, где каждая SCC сжата в одну вершину; рёбра между компонентами. Конденсация — DAG.

Почему конденсация ациклична?

Цикл между SCC означал бы, что их можно объединить в одну сильную компоненту.

Сложность поиска SCC Косарайю?

O(V+E).

Структуры данных для SCC

Два списка смежности G и H, visited/component, стек времён выхода.

Применимость конденсации

Анализ достижимости «с точностью до циклов», 2-SAT, упрощение орграфов.

Конденсация по SCC всегда является [...].

Конденсация по SCC всегда является DAG.

***Как обнаружить цикл в орграфе за O(V+E)?***

DFS + цвета: ребро в gray ⇒ цикл.

Как восстановить цикл по parent?

При back edge (v→u): идти от v по parent до u, собрать вершины, замкнуть u.

Что такое фундаментальный цикл относительно DFS-дерева?

Цикл из одного back edge + путь по дереву между его концами.

Цикломатическое число связного неориентированного графа?

E − V + 1 — размер базиса циклов.

Почему перечислить ВСЕ простые циклы дорого?

Их может быть экспоненциально много (алгоритм Джонсона и т.п.).

Как найти отрицательный цикл Беллманом–Фордом?

После V−1 релаксаций ещё один проход: если ребро релаксируется — вершина на/достижима из отриц. цикла; восстановить по parent.

Как Флойд–Уоршалл детектит отрицательный цикл?

На диагонали изначально 0; если после алгоритма W[i][i] < 0 — i на отрицательном цикле.

Отличие: цикл vs Эйлеров vs Гамильтонов

Цикл — замкнутый путь; Эйлеров — по всем рёбрам; Гамильтонов — по всем вершинам.

Применимость поиска циклов

Проверка DAG, дедлоки зависимостей, арбитраж (отриц. цикл), базисы циклов.

В орграфе ребро DFS в [...] вершину означает цикл.

В орграфе ребро DFS в серую вершину означает цикл.

***Что такое гамильтонов цикл?***

Простой цикл, проходящий каждую вершину ровно один раз и возвращающийся в старт.

Сложность задачи Гамильтонова цикла?

NP-полная (поиск/существование трудны в общем случае).

Достаточное условие Дирака

Если n≥3 и deg(v) ≥ n/2 для всех v — есть гамильтонов цикл.

Достаточное условие Оре

Если n≥3 и для любых несмежных u,v: deg(u)+deg(v) ≥ n — есть гамильтонов цикл.

Дирак ⇒ Оре?

Да, условие Дирака сильнее (следует из Оре наоборот не всегда нужно помнить строго: Дирак — частный случай идеи Оре).

Как искать гамильтонов цикл при выполнении достаточных условий / на практике?

Бэктрекинг/перебор с отсечениями; при условиях Дирака/Оре существование гарантировано, построение — конструктивные доказательства/эвристики.

Гамильтонов путь vs цикл

Путь проходит все вершины по разу без возврата; цикл — с возвратом.

Ограничения

Условия Дирака/Оре достаточны, но не необходимы; без условий полный перебор экспоненциален.

Применимость

Маршруты «посетить все города», задачи коммивояжёра (связь), теоретические критерии.

Связь с топосортом (лекция)

В орграфах топосорт помогает искать гамильтонов путь (проверка рёбер между соседними в порядке).

По Дираку нужно deg(v) ≥ [...] для всех вершин (n≥3).

По Дираку нужно deg(v) ≥ n/2 для всех вершин (n≥3).

***Что такое эйлеров цикл?***

Замкнутый маршрут, проходящий каждое ребро ровно один раз.

Критерий эйлерова цикла в неориентированном графе

Граф связен (игнорируя изолированные) и все степени чётные.

Критерий эйлерова цикла в орграфе

Сильно связен (слабо + баланс) и для каждой вершины indeg = outdeg.

Эйлеров путь (не цикл) в неориентированном

Ровно 0 или 2 вершины нечётной степени (если 2 — путь между ними).

Алгоритм Иерохольцера (идея)

Идём unused-рёбрами, строим циклы и склеиваем их в один обход; O(E).

Сложность поиска эйлерова цикла

O(E) (при аккуратной реализации удаления рёбер).

Структуры данных

Список смежности с удаляемыми рёбрами (итераторы/стек рёбер), стек текущего пути.

Ограничения

Нужна связность нужного типа; мультиграфы ок; не путать с гамильтоновым.

Применимость

Китайский почтальон (связан), обход улиц/проводов, реконструкция строк из k-меров (де Брёйна).

Для эйлерова цикла в неориентированном графе все степени должны быть [...].

Для эйлерова цикла в неориентированном графе все степени должны быть чётными.

***Что такое компонента связности?***

Максимальное множество вершин, попарно достижимых друг из друга в неориентированном графе.

Как найти все КС?

Серия DFS или BFS: из каждой непосещённой вершины новый обход, помечаем номером компоненты.

Сложность?

O(V+E).

Структуры

Список смежности, visited/component[], счётчик компонент.

Можно ли BFS вместо DFS?

Да, полностью аналогично.

Поиск компонент связности работает за [...].

Поиск компонент связности работает за O(V+E).

***Что строит алгоритм Крускала?***

Минимальное остовное дерево (MST) / лес во взвешенном неориентированном графе.

Идея Крускала в одном предложении

Сортируем рёбра по весу и добавляем, если не создаёт цикл (DSU).

Зачем DSU/СНМ?

Проверка «разные ли компоненты» и объединение за почти O(1) амортизированно.

Сложность Крускала

O(E log E) из-за сортировки (DSU почти линеен).

Память Крускала

O(V+E) на рёбра и DSU.

Можно ли отрицательные веса?

Да, для MST знак не мешает.

Что если граф несвязный?

Получится минимальный остовный лес.

Когда ребро отвергается?

Если концы уже в одной компоненте DSU ⇒ цикл.

Пример из лекции: ae=1, cd=2, ab=3, be=4 — что с be?

be отвергается: цикл a-e-b-a.

Свойство разреза (идея корректности)

Самое лёгкое ребро через разрез безопасно для некоторого MST.

Чем Крускал отличается от Прима?

Крускал — глобально по рёбрам + DSU; Прим — растит одно дерево из вершины.

Структуры данных Крускала

Массив рёбер (w,u,v), DSU (parent, rank), ответ — список рёбер.

Ограничения

Неориентированный граф; для ориентированных — другие задачи (ветвления).

Применимость

Сети минимальной стоимости, кластеризация через MST, остовы.

Петля (v,v) в Крускале?

Всегда цикл, никогда не берём.

Параллельные рёбра?

Сортировка выберет более лёгкие; тяжёлые отбросятся.

Сколько рёбер в MST связного графа?

V − 1.

Почему сортировка доминирует?

DSU на E операций ≈ O(E α(V)), а sort — O(E log E).

Можно ли проверять цикл DFS на каждом ребре?

Можно, но получится медленнее; DSU — правильный путь.

Жадность Крускала корректна потому что…

Безопасные рёбра разреза / матроидные свойства MST.

Крускал: сложность [...], проверка циклов через DSU/СНМ.

Крускал: сложность O(E log E), проверка циклов через DSU/СНМ.

Крускал: сложность O(E log E), проверка циклов через [...].

Крускал: сложность O(E log E), проверка циклов через DSU/СНМ.

***Идея алгоритма Прима***

Растим дерево из стартовой вершины, каждый раз добавляя минимальное ребро из дерева наружу.

Сложность наивного Прима

O(V²) — на каждом шаге поиск минимума по массиву.

Сложность Прима с бинарной кучей

O(E log V) (или O((V+E) log V)).

Какие массивы нужны?

minEdge[v], used[v], parent[v] (чтобы восстановить рёбра MST!).

Почему важен parent[] (лекция)?

Иначе знаем только расстояния до дерева, но не какие рёбра вошли в MST.

Ограничения Прима

Неориентированный связный (иначе лес по компоненте); обычно неотрицательность не требуется для MST.

Применимость

MST; удобен на плотных графах в версии O(V²).

Сходство с Дейкстрой

Похожая жадная схема с ключами вершин, но семантика — ребро в MST, не путь от s.

Структуры

Список смежности / матрица; куча или массив минимумов.

Корректность (разрез)

Минимальное ребро из S в V\S безопасно для MST.

Прим с кучей: [...]; наивный: O(V²).

Прим с кучей: O(E log V); наивный: O(V²).

Прим с кучей: O(E log V); наивный: [...].

Прим с кучей: O(E log V); наивный: O(V²).

***Что находит Беллман–Форд?***

Кратчайшие пути от одного источника; допускает отрицательные веса.

Основной шаг алгоритма

Релаксация всех рёбер; повторить |V|−1 раз.

Зачем ещё один (V-й) проход?

Если релаксация ещё возможна — есть отрицательный цикл, достижимый из s.

Сложность BF

O(V·E) время, O(V) память на dist/parent.

Структуры

Список рёбер (удобно) или списки смежности; dist[], parent[].

Ограничения

Медленнее Дейкстры на неотрицательных; при отриц. цикле на пути расстояния не определены (−∞).

Применимость

Отрицательные веса, детект арбитража/отриц. циклов, разностные ограничения.

Релаксация ребра (u,v,w)

если dist[v] > dist[u] + w → dist[v] = dist[u] + w, parent[v]=u.

Инициализация

dist[s]=0, остальные +∞.

Почему достаточно V−1 итераций?

Простой кратчайший путь ≤ V−1 рёбер (без отриц. циклов).

Восстановление отрицательного цикла

Из вершины, улучшенной на V-м проходе, пройтись по parent V раз, затем собрать цикл.

Работает ли BF на неориентированном с отриц. ребром?

Отриц. неориентированное ребро = цикл длины 2 с отриц. весом — обычно проблема; модель осторожно.

Сравнение с Дейкстрой

BF: есть отрицательные, O(VE); Дейкстра: только ≥0, быстрее с кучей.

Можно ли остановиться раньше?

Да, если на итерации не было улучшений.

Память

O(V) + граф.

Беллман–Форд делает [...] полных релаксаций, плюс ещё одну для отрицательного цикла.

Беллман–Форд делает |V|−1 полных релаксаций, плюс ещё одну для отрицательного цикла.

Беллман–Форд делает |V|−1 полных релаксаций, плюс ещё одну для [...].

Беллман–Форд делает |V|−1 полных релаксаций, плюс ещё одну для отрицательного цикла.

***Кратчайшие пути в DAG: идея***

Топосорт, затем одна релаксация исходящих рёбер в этом порядке.

Сложность

O(V+E).

Можно ли отрицательные веса в DAG SP?

Да, циклов нет ⇒ отрицательные рёбра допустимы.

Пример лекции: старт 3, порядок 3,5,1,4,2 — суть

Релаксируем в топопорядке от старта; получаем конечные dist.

Если старт не «первая» в топосорте?

Вершины до старта в порядке останутся ∞, если нет пути из старта.

Структуры

Список смежности, order[], dist[], parent[]; топосорт DFS или Демукрон.

Кратчайшие пути в DAG работают за [...] благодаря топологической сортировке.

Кратчайшие пути в DAG работают за O(V+E) благодаря топологической сортировке.

Кратчайшие пути в DAG работают за O(V+E) благодаря [...].

Кратчайшие пути в DAG работают за O(V+E) благодаря топологической сортировке.

***Что делает Дейкстра?***

Кратчайшие пути от s до всех при неотрицательных весах (жадный алгоритм).

Главное ограничение Дейкстры

Не работает с отрицательными весами рёбер.

Наивная реализация: сложность

O(V²) — каждый раз искать вершину с min dist.

С бинарной кучей: сложность

O((V+E) log V).

Структуры

Список смежности, dist[], used/visited, parent[], priority_queue.

Лемма: частичные пути кратчайших путей…

…тоже кратчайшие (доказательство заменой подпути).

Сходство с Примом

Похожий жадный выбор вершин, но оптимизируем расстояние от s, не ребро MST.

Применимость

Карты/маршруты с неотрицательными длинами, графы состояний.

Почему жадность работает при w≥0?

Выбранная вершина с минимальным dist уже имеет окончательный кратчайший путь.

Дейкстра требует веса [...]; с кучей сложность O((V+E) log V).

Дейкстра требует веса ≥ 0; с кучей сложность O((V+E) log V).

Дейкстра требует веса ≥ 0; с кучей сложность [...].

Дейкстра требует веса ≥ 0; с кучей сложность O((V+E) log V).

***Что находит Флойд–Уоршалл?***

Кратчайшие пути между всеми парами вершин.

Порядок трёх циклов (лекция)

1) промежуточные k; 2) начала i; 3) концы j.

Формула релаксации

dist[i][j] = min(dist[i][j], dist[i][k] + dist[k][j]).

Сложность времени и памяти

O(V³) время, O(V²) память (матрица).

Отрицательные рёбра?

Да; отрицательные циклы детектятся по dist[i][i] < 0.

Инициализация диагонали

W[i][i] = 0 (важно для детекта циклов).

DP-смысл состояния

Кратчайший путь i→j с промежуточными из {1..k}.

Ограничения

Тяжело на больших V; при отриц. цикле часть ответов −∞.

Применимость

Плотные небольшие графы, APSP, транзитивное замыкание (вариант).

Структура данных

Матрица смежности/расстояний V×V, опционально next[][] для путей.

Флойд–Уоршалл: [...], внешний цикл по промежуточной вершине k.

Флойд–Уоршалл: O(V³), внешний цикл по промежуточной вершине k.

Флойд–Уоршалл: O(V³), внешний цикл по [...] вершине k.

Флойд–Уоршалл: O(V³), внешний цикл по промежуточной вершине k.

***Что такое диаметр дерева?***

Максимальное расстояние (по рёбрам или весу) между двумя вершинами; длина длиннейшего простого пути.

Алгоритм двумя BFS/DFS

1) из любой вершины найти самую дальнюю A; 2) из A найти самую дальнюю B; dist(A,B) — диаметр.

Почему два обхода достаточно?

В дереве путь единственный; самая дальняя от произвольной лежит на диаметре.

Сложность

O(V) (два обхода дерева, E=V−1).

Структуры

Список смежности дерева, очередь BFS или рекурсия DFS, массивы dist.

Ограничения

Только деревья (или компоненты-деревья); для общих графов диаметр сложнее.

Диаметр дерева находится за [...] BFS/DFS, итог O(V).

Диаметр дерева находится за два BFS/DFS, итог O(V).

Диаметр дерева находится за два BFS/DFS, итог [...].

Диаметр дерева находится за два BFS/DFS, итог O(V).

***Что ищет алгоритм Куна?***

Максимальное паросочетание в двудольном графе.

Что такое паросочетание?

Множество рёбер без общих вершин.

Увеличивающая цепь

Чередующаяся цепь, начинающаяся и заканчивающаяся свободными вершинами.

Теорема Бержа

Паросочетание максимально ⟺ нет увеличивающей цепи.

Идея Куна

Для каждой свободной вершины левой доли DFS ищет увеличивающую цепь; нашли — инвертируем рёбра.

Сложность Куна

O(V·E).

Структуры

Списки смежности доли L→R, массив пары pairR[u]/pairL[v], used[] на итерацию.

Ограничения

Только двудольные графы (для общих — Эдмондс matching сложнее).

Кун работает за [...] и опирается на увеличивающие цепи / теорему Бержа.

Кун работает за O(V E) и опирается на увеличивающие цепи / теорему Бержа.

Кун работает за O(V E) и опирается на [...] / теорему Бержа.

Кун работает за O(V E) и опирается на увеличивающие цепи / теорему Бержа.

***Три свойства потока (лекция)***

Ограничение f≤c; антисимметрия f(u,v)=−f(v,u); сохранение Σf=0 для v≠s,t.

Остаточная сеть: прямое и обратное ребро

Прямое: c−f; обратное: f (можно отменить поток).

Идея Форда–Фалкерсона

Пока есть s-t путь в остаточной — пустить bottleneck, обновить остатки.

Сложность FF с DFS

O(E · |f*|) — зависит от величины потока; на иррациональных может не завершиться.

Пример-убийца DFS

Рёбра 1000 и узкое 1: DFS может качать по 1 много раз; BFS — за 2 итерации.

Эдмондс–Карп = ?

Форд–Фалкерсон + выбор кратчайшего (по числу рёбер) пути через BFS.

Сложность Эдмондса–Карпа

O(V E²).

Идея доказательства O(VE) итераций

Ребро «умирает» O(V) раз; рост кратчайших расстояний d(v).

Теорема max-flow = min-cut

Величина макс. потока = пропускная способность мин. разреза.

Как построить мин. разрез после макс. потока?

S = достижимые из s в остаточной сети; рёбра S→T насыщены.

Величина потока |f|

Сумма потока из источника (или в сток).

Структуры для потока

Матрица cap[][] / списки остаточных рёбер; parent для пути.

Эдмондс–Карп: [...]; классический FF: O(E |f*|).

Эдмондс–Карп: O(V E²); классический FF: O(E |f*|).

Эдмондс–Карп: O(V E²); классический FF: [...].

Эдмондс–Карп: O(V E²); классический FF: O(E |f*|).

***Как свести двудольное паросочетание к потоку?***

Добавить s→L (cap 1), рёбра L→R (cap 1), R→t (cap 1); макс. поток = макс. паросочетание.

Почему пропускные способности 1?

Каждая вершина используется не более одного раза в паросочетании.

Какой алгоритм потока удобен?

Эдмондс–Карп / Диниц; на единичных сетях быстро; либо Кун напрямую.

Связь с Куном

Оба ищут увеличивающие цепи; Кун — специализированный DFS на двудольном.

При сведении паросочетания к потоку все пропускные способности равны [...].

При сведении паросочетания к потоку все пропускные способности равны 1.

***Что такое мост?***

Ребро, удаление которого увеличивает число компонент связности.

Что такое точка сочленения?

Вершина, удаление которой увеличивает число компонент.

Массивы tin и low (up) в DFS

tin — время входа; low — мин. достижимое tin через дерево и back edges.

Критерий моста (u,v) дерево-ребро

low[v] > tin[u] (из поддерева v нельзя вернуться к u или выше).

Критерий точки сочленения

Корень с ≥2 детьми в DFS-дереве; не-корень: ∃ ребёнок v с low[v] ≥ tin[u].

Сложность

O(V+E) одним DFS.

В дереве каждое ребро — ?

Мост (лекция про деревья).

Применимость

Надёжность сетей, двусвязные компоненты, точки отказа.

Ребро (u,v) — мост, если [...].

Ребро (u,v) — мост, если low[v] > tin[u].

***Идея жадной раскраски***

Идём по вершинам в каком-то порядке, красим минимальным цветом, не конфликтующим с уже окрашенными соседями.

Оценка χ ≤ ?

χ(G) ≤ Δ+1, где Δ — макс. степень (жадная даёт ≤ Δ+1).

Теорема Брукса (упомянуть)

Для связного графа, кроме полного и нечётного цикла, χ ≤ Δ.

Сложность жадной

O(V+E) при аккуратной реализации.

Ограничения

Зависит от порядка вершин; не всегда оптимальна (раскраска NP-трудна).

Применимость

Регистры, расписание, приближённая раскраска.

Жадная раскраска гарантирует не более [...] цветов.

Жадная раскраска гарантирует не более Δ+1 цветов.

***Идея Рабина–Карпа***

Сравниваем полиномиальные хеши образца и окон текста; при равенстве хешей — проверка.

Формула сдвига окна (лекция)

H_{i+1} = (H_i · X − t_i · X^m + t_{i+m}) mod K.

Итеративное hash(p+c)

(hash(p)·X + c) mod K.

Сложность ожидаемая

O(n+m) при малых коллизиях; худший O(nm) при постоянных коллизиях.

Параметры X и K

K — большое простое; X взаимно просто с K / больше алфавита.

Сдвиг окна хеша за [...]: умножить на X, вычесть ушедший символ, добавить новый.

Сдвиг окна хеша за O(1): умножить на X, вычесть ушедший символ, добавить новый.

***Что такое префикс-функция π[i]?***

Длина наибольшего собственного префикса s[0..i], который является суффиксом s[0..i].

Сложность вычисления π

O(n).

Зачем нужна?

КМП, периоды строки, сжатие границ совпадений.

Как используют π при несовпадении в КМП?

Сдвигаемся на π[j−1] (новый кандидат длины совпавшего префикса), не откатываем указатель текста назад.

π[0] всегда?

0 (собственный префикс пустой).

Связь π с периодом строки

Если n − π[n−1] делит n, то есть период длины n−π[n−1].

Префикс-функция считается за [...].

Префикс-функция считается за O(n).

***Определение z[i]***

Длина наибольшего префикса S, совпадающего с подстрокой от i; z[0]=0.

Пример domodedovo: z[4] и z[6]?

z[4]=1 (d…), z[6]=2 (do…).

Что такое z-блок [L,R]?

Отрезок, совпадающий с префиксом, с максимальным R.

Сложность умного алгоритма vs наивного

O(n) vs O(n²).

Если i внутри [L,R], откуда берём начальный z[i]?

Из z[i−L], не выходя за границу R (иначе досчитываем сравнениями).

Поиск P в T через Z

Считаем Z от P+'#'+T; вхождения где z[i]=|P|.

Z-функция: наивно [...], с z-блоком O(n).

Z-функция: наивно O(n²), с z-блоком O(n).

Z-функция: наивно O(n²), с z-блоком [...].

Z-функция: наивно O(n²), с z-блоком O(n).

***Идея КМП***

Автомат/сдвиги по префикс-функции образца — не сравниваем лишнее после несовпадения.

Сложность КМП

O(n+m).

Строка для π в классическом КМП поиска

Часто P + '#' + T или отдельно π(P) и проход по T.

Почему КМП лучше наивного?

Не откатывает указатель по тексту: каждый символ T обрабатывается амортизированно O(1).

Что хранит состояние КМП во время прохода?

Текущая длина совпавшего префикса P (индекс в образце).

КМП работает за [...] благодаря префикс-функции.

КМП работает за O(n+m) благодаря префикс-функции.

КМП работает за O(n+m) благодаря [...].

КМП работает за O(n+m) благодаря префикс-функции.

***Идея автомата поиска подстроки***

Состояния = длины текущего совпавшего префикса образца; переходы по символам.

Связь с КМП

По сути тот же автомат, построенный по π / явным переходам δ(state, c).

Сложность построения/работы

Построение O(m|Σ|) или лениво; проход по тексту O(n).

Что в терминальном состоянии?

Прочитан весь образец — найдено вхождение.

Зачем явные δ(state,c), если есть π?

O(1) переход без цикла по π на каждом символе; удобно при большом тексте.

Автомат поиска подстроки имеет состояния = [...].

Автомат поиска подстроки имеет состояния = префиксы образца.

***Что такое бор (trie)?***

Дерево префиксов множества строк; в вершине — терминальность и переходы по символам.

Память бора

O(Σ |S_i|) вершин в худшем случае.

Добавление/поиск строки

Спуск с созданием/следованием переходов: O(|S|) или O(|S| log|Σ|) с map.

map vs unordered_map vs массив на Σ

map: log|Σ|; unordered: средний O(1); массив: O(1), память ×|Σ|.

Плюс против set<string>

Общие префиксы экономят память; удобен поиск по префиксу.

Бор также называют [...] / trie.

Бор также называют префиксным деревом / trie.

***Что такое хеш-функция (лекция)?***

Преобразование данных в короткий образ (хеш); свойства: уникальность (стремимся), необратимость, изменяемость, быстрая вычислимость.

Что такое хеш-таблица?

Структура словаря (ключ→значение) с ожидаемыми O(1) insert/find/delete.

Что такое коллизия?

∃ x≠y: hash(x)=hash(y).

Метод деления

h(key)=key % m, m желательно простое.

Метод умножения: константа A

A ≈ (√5−1)/2 (золотое сечение); h = ⌊m · {key·A}⌋.

Полиномиальный хеш строки

Σ s_i k^i mod p (или старшие степени слева).

Сравнение с BST по операциям

Хеш: ожидаемо O(1); BST: O(log n) гарантии порядка.

Где применяется хеширование?

Словари, пароли/отпечатки, БД, блокчейн, антивирусы, подписи (криптохеши).

Коллизия: разные ключи с [...] значением хеш-функции.

Коллизия: разные ключи с одинаковым значением хеш-функции.

***Закрытая адресация (метод цепочек)***

В ячейке список (цепь) всех ключей с данным хешем.

Открытая адресация

Все ключи в массиве таблицы; при коллизии ищем другую свободную ячейку по правилу пробинга.

Линейное пробирование

h(k)+i) mod m, i=0,1,2,...

Плюсы цепочек

Простое удаление; таблица может быть меньше числа ключей; меньше чувствительности к кластеризации.

Минусы открытой адресации

Нужен load factor <1; удаление сложнее (tombstones); первичная кластеризация.

Пример лекции H(k)=k mod 11, ключи 23,1,12,...

Много ключей с остатком 1 попадают в одну цепь/кластер — иллюстрация коллизий.

Метод цепочек = [...] адресация; пробирование в массиве = открытая.

Метод цепочек = закрытая адресация; пробирование в массиве = открытая.

Метод цепочек = закрытая адресация; пробирование в массиве = [...].

Метод цепочек = закрытая адресация; пробирование в массиве = открытая.

***Идея кукушкиного хеширования***

Две хеш-функции / две таблицы; ключ вытесняет («kick») другого на альтернативное место.

Сложность contains/remove

O(1) — проверка двух позиций.

Что такое зацикливание?

В процессе вставки вернулись к той же ячейке/конфигурации (пример: 3 ключа с одинаковыми h1 и h2).

Что делать при цикле?

Новые хеш-функции и rehash.

Пример h1=x%8, h2=⌊x/8⌋%8: вставка 42 при занятых местах

Кладут 42 с вытеснением (напр. 10), затем 10 переезжает на h2.

Кукушка: поиск за [...] (две позиции); при цикле — перехеширование.

Кукушка: поиск за O(1) (две позиции); при цикле — перехеширование.

Кукушка: поиск за O(1) (две позиции); при цикле — [...].

Кукушка: поиск за O(1) (две позиции); при цикле — перехеширование.

***Что такое фильтр Блума?***

Вероятностное множество: компактная проверка принадлежности с возможными false positive, без false negative.

Структура

Битсет длины m + k хеш-функций в {0..m−1}.

Поддерживается ли удаление?

В классическом — нет (нужен counting Bloom).

Как растёт ошибка?

Больше элементов / меньше m → выше P(false positive).

Пример мотивации (лекция)

Чёрный список IP: нельзя хранить бит на каждый из 2^32 адресов.

Применимость

Поиск/кэши, криптокошельки, ДНК, отсечение заведомо отсутствующих ключей.

Блум: возможны [...], невозможны ложноотрицательные.

Блум: возможны ложноположительные, невозможны ложноотрицательные.

Блум: возможны ложноположительные, невозможны [...].

Блум: возможны ложноположительные, невозможны ложноотрицательные.

***Идея масштабирования потока***

Качать порции ≥ 2^k, начиная с k=⌊log₂ C⌋, затем уменьшать k.

Сложность (лекция)

O(E² log C).

Сравнение FF / EK / scaling

FF: O(E|f*|); EK: O(V E²); scaling: O(E² log C), слабо зависит от весов.

Масштабирование потока: [...].

Масштабирование потока: O(E² log C).

***Что такое суффиксный массив?***

Перестановка индексов начал суффиксов строки в лексикографическом порядке.

Зачем нужен?

Поиск подстроки бинпоиском O(m log n), LCP, сравнение с суффиксным деревом по памяти.

Сложность построения (типично)

O(n log n) простые удвоения; есть O(n).

Суффиксный массив — упорядоченные [...] строки.

Суффиксный массив — упорядоченные суффиксы строки.

***Что такое суффиксное дерево?***

Сжатое дерево всех суффиксов строки (бор суффиксов со сжатыми рёбрами).

Связь с суффиксным массивом

Массив — «плоская» версия листьев дерева в порядке; LCP связан с глубинами.

Применимость

Много запросов о подстроках, наименьший цикл, повторы; Ukkonen O(n).

Суффиксное дерево хранит все [...] в сжатом виде.

Суффиксное дерево хранит все суффиксы в сжатом виде.

***Задача Ахо–Корасик***

Найти все вхождения множества шаблонов в текст за один проход.

Что добавляют к бору?

Суффиксные ссылки, автоматные переходы go(v,c), выходные (терминальные) ссылки.

Сложность

O(Σ|P_i| · |Σ| + |T| + размер ответа) типично.

Аналогия с КМП

Суффиксные ссылки = обобщение «отката» префикс-функции на много слов.

Ахо–Корасик = бор + [...] + переходы автомата.

Ахо–Корасик = бор + суффиксные ссылки + переходы автомата.
