# 21. Полиномиальный хеш и алгоритм Рабина-Карпа

## Задача поиска подстроки

Дан текст `T[0..n-1]` и образец `P[0..m-1]`, где `m <= n`. Нужно найти все позиции `i`, такие что `T[i..i+m-1] = P`.

Наивный алгоритм пробует каждую позицию начала и сравнивает до `m` символов. Его сложность `O(m * (n - m + 1))`, потому что информация о предыдущих сравнениях не используется.

Идея Рабина-Карпа: сравнивать не сами строки, а их хеши. Если `h(x) != h(y)`, то точно `x != y`. Если `h(x) = h(y)`, строки, скорее всего, равны, но возможна коллизия, поэтому при строгом алгоритме нужно дополнительно проверить символы.

## Полиномиальный хеш

Полиномиальный хеш представляет строку как число в системе счисления с основанием `X`, а затем берет остаток по модулю `K`.

В лекциях встречаются две эквивалентные записи:

```text
hash(s) = (s0 * X^(m-1) + s1 * X^(m-2) + ... + s(m-2) * X + s(m-1)) mod K
```

и

```text
hash(s) = sum s_i * k^i mod p
```

Разница только в направлении степеней. Главное, чтобы вычисление хеша образца и всех окон текста использовало одну и ту же договоренность.

Параметры:

- `s_i` - числовой код символа.
- `X` или `k` - основание, обычно больше размера алфавита.
- `K` или `p` - модуль, обычно большое простое число.
- `X` желательно взаимно просто с `K`.

Хеш не является доказательством равенства строк: разные строки могут иметь одинаковый остаток по модулю.

## Итеративное вычисление

Если хеш записан старшими степенями слева, то при добавлении символа `c` справа:

```text
hash(p + c) = (hash(p) * X + c) mod K
```

Это важный прием из лекции: вместо пересчета всей строки за `O(m)` каждый новый символ добавляется за `O(1)`.

Пример из лекции для `X = 2`, `K = 5`, алфавит `a..z`, строка `hash`:

```text
h(h)    = 7 mod 5 = 2
h(ha)   = (2 * 2 + 0) mod 5 = 4
h(has)  = (4 * 2 + 18) mod 5 = 1
h(hash) = (1 * 2 + 7) mod 5 = 4
```

Предварительный подсчет хеша строки длины `m` занимает `O(m)`, добавление очередного символа - `O(1)`.

## Пример `computer`

В лекции дан пример:

```text
X = 2, K = 5, alphabet = [a..z]
hash("computer") =
(2 * 2^7 + 14 * 2^6 + 12 * 2^5 + 15 * 2^4 +
 20 * 2^3 + 19 * 2^2 + 4 * 2^1 + 17) mod 5 = 2
```

Такой маленький модуль `K = 5` удобен для ручного счета, но плох в реальных задачах: слишком много разных строк будут попадать в один из пяти хешей.

## Сдвиг окна

Пусть есть окно текста длины `m`:

```text
H_i = hash(T[i..i+m-1])
```

Тогда следующее окно:

```text
H_(i+1) = hash(T[i+1..i+m])
```

Формула пересчета из лекции:

```text
H_(i+1) = (H_i * X - t_i * X^m + t_(i+m)) mod K
```

Смысл:

1. Умножаем старый хеш на `X`, сдвигая степени.
2. Удаляем вклад символа `t_i`, который вышел из окна.
3. Добавляем новый символ `t_(i+m)`.
4. Берем модуль `K`.

В реализации важно нормализовать отрицательный остаток:

```text
H = ((H % K) + K) % K
```

## Алгоритм Рабина-Карпа

1. Посчитать `hp = hash(P)` за `O(m)`.
2. Посчитать хеш первого окна `T[0..m-1]` за `O(m)`.
3. Для каждого окна текста:
   - если хеш окна равен `hp`, проверить строку посимвольно или принять как вероятное совпадение;
   - перейти к следующему окну по формуле сдвига.

Псевдокод:

```text
RabinKarp(T, P):
    n = len(T), m = len(P)
    hp = hash(P)
    ht = hash(T[0..m-1])
    ans = []

    for i = 0..n-m:
        if ht == hp:
            if T[i..i+m-1] == P:
                ans.push(i)
        if i < n-m:
            ht = (ht * X - code(T[i]) * X^m + code(T[i+m])) mod K

    return ans
```

## Сложность

- Предподсчет хеша образца: `O(m)`.
- Предподсчет первого окна: `O(m)`.
- Пересчет всех остальных окон: `O(n - m)`.
- Итого без учета проверок коллизий: `O(n + m)`.
- С проверкой символов в каждой коллизии худший случай может стать `O(nm)`, если хеш-функция плохая или данные специально подобраны.
- Память: `O(1)` плюс массив ответа.

На практике используют большой модуль, иногда два модуля или 64-битный переполняющийся хеш, чтобы вероятность ложного совпадения была мала.

## Структуры данных

- Числа для хешей.
- Массив степеней `X^i mod K`, если нужны быстрые подстрочные хеши.
- Массив префиксных хешей, если нужно сравнивать произвольные подстроки, а не только двигать окно.
- Массив ответов с позициями вхождений.

## Ограничения и риски

- Коллизии неизбежны, потому что строк бесконечно много, а хешей конечное число.
- Малый модуль резко увеличивает вероятность ложных совпадений.
- Нужно аккуратно работать с отрицательными остатками.
- Нужно следить за переполнением, если язык не делает его контролируемым.
- В задачах, где требуется абсолютная корректность, совпадение хешей надо подтверждать сравнением строк.

## Когда применять

Рабин-Карп удобен:

- при поиске одного образца в тексте;
- при поиске многих образцов одинаковой длины;
- когда нужна вероятностно быстрая проверка равенства подстрок;
- в задачах на дубликаты, плагиат, rolling hash, сравнение фрагментов.

Менее удобен, если:

- нужна строгая гарантия без дополнительной проверки;
- противник может подбирать входы под хеш;
- алфавит и модуль выбраны неудачно.

## Экзамен-вопросы

1. Почему из `h(x) != h(y)` следует `x != y`, но из `h(x) = h(y)` не следует `x = y`?
2. Выведите формулу `H_(i+1) = (H_i * X - t_i * X^m + t_(i+m)) mod K`.
3. Почему первый хеш считается за `O(m)`, а каждый следующий за `O(1)`?
4. Что такое коллизия и как она влияет на Рабина-Карпа?
5. Почему в лекционном примере `K = 5` плох для реального поиска?
6. Какая сложность у Рабина-Карпа в среднем и в худшем случае?
7. Чем отличаются записи `sum s_i * k^i mod p` и запись со степенями от `m-1` до `0`?
