# 24. КМП: алгоритм Кнута-Морриса-Пратта

## Задача

Дан текст `T` длины `n` и образец `P` длины `m`. Нужно найти все вхождения `P` в `T`.

КМП решает задачу за:

```text
O(n + m)
```

Главная идея: если сравнение оборвалось после нескольких совпавших символов, нельзя выкидывать эту информацию. Нужно понять, какой префикс образца уже может быть суффиксом прочитанной части текста.

Эту информацию дает префикс-функция.

## Почему наивный поиск плох

Наивный поиск для каждой позиции текста заново сравнивает образец:

```text
for start = 0..n-m:
    compare T[start..start+m-1] with P
```

В худшем случае:

```text
O(m * (n - m + 1))
```

Например, на тексте `aaaaaaaaab` и образце `aaaab` много сравнений повторяются.

## Предподсчет

Сначала считаем префикс-функцию для образца `P`:

```text
pi[i] = длина максимального собственного префикса P[0..i],
        который является суффиксом P[0..i]
```

Пример из лекции:

```text
P:  A B A C A B A B A C B
pi: 0 0 1 0 1 2 3 2 3 4 0
```

Префикс-функция подсказывает, куда откатиться внутри образца при несовпадении.

## Основная логика КМП

Храним `k` - сколько первых символов образца уже совпало с суффиксом обработанной части текста.

При чтении очередного символа `T[i]`:

1. Пока `k > 0` и `P[k] != T[i]`, откатываемся:

```text
k = pi[k - 1]
```

2. Если `P[k] == T[i]`, увеличиваем:

```text
k += 1
```

3. Если `k == m`, найдено вхождение, заканчивающееся в `i`. Его начало:

```text
i - m + 1
```

4. После найденного вхождения нужно откатиться:

```text
k = pi[m - 1]
```

Это позволяет находить перекрывающиеся вхождения.

## Псевдокод

```text
KMP(T, P):
    pi = prefix_function(P)
    k = 0
    ans = []

    for i = 0..len(T)-1:
        while k > 0 and P[k] != T[i]:
            k = pi[k - 1]

        if P[k] == T[i]:
            k += 1

        if k == len(P):
            ans.push(i - len(P) + 1)
            k = pi[k - 1]

    return ans
```

В реальном коде нужно отдельно обработать случай пустого образца, потому что обращение `P[k]` некорректно при `m = 0`.

## Пример из лекции

В лекции показан потоковый пример с образцом:

```text
P = abaca
pi = 0 0 1 0 1
```

Текст читается по символам. Переменная `k` меняется так:

```text
T[0] = P[0], k: 0 -> 1
T[1] = P[1], k: 1 -> 2
T[2] = P[2], k: 2 -> 3
T[3] != P[3], k: 3 -> pi[2] = 1
T[3] = P[1], k: 1 -> 2
...
T[6] = P[4], k: 4 -> 5 = |P|
Ответ: 6 - |P| + 1 = 2
```

Смысл отката: мы не возвращаемся в тексте назад. Мы только уменьшаем длину текущего совпадения в образце.

## Поиск через `P#T`

Можно искать и через префикс-функцию объединенной строки:

```text
S = P + "#" + T
```

Если в `S` значение `pi[i] == |P|`, то найдено вхождение. Такой способ проще для понимания, но хранит всю строку `P#T`.

Классический КМП хранит только:

- образец `P`;
- `pi` для образца;
- текущее `k`.

Поэтому он удобнее для потока.

## Сложность

Предподсчет:

```text
O(m)
```

Проход по тексту:

```text
O(n)
```

Итого:

```text
O(n + m)
```

Память:

```text
O(m)
```

Если хранить ответы, добавляется `O(result)`.

## Почему проход по тексту линейный

`i` увеличивается на каждой итерации внешнего цикла и никогда не уменьшается. Переменная `k` может увеличиться не больше `n` раз за весь проход, потому что увеличивается только при совпадении с очередным символом текста.

Откаты `k = pi[k-1]` суммарно тоже ограничены количеством предыдущих увеличений `k`. Поэтому внутренний `while` не делает алгоритм квадратичным.

## Структуры данных

- Строка образца `P`.
- Массив `pi` длины `m`.
- Переменная `k`.
- Список найденных позиций.

## Ограничения

- КМП ищет точные совпадения.
- Для множества образцов КМП придется запускать для каждого образца, что даст `O(sum |P_i| + q * |T|)`. Лучше использовать Ахо-Корасик.
- Для небольших строк на практике наивный алгоритм может быть проще, но асимптотически хуже.
- Нужна аккуратная обработка пустого образца.

## Применимость

КМП применяют:

- в текстовых редакторах для поиска;
- в задачах на строки и шаблоны;
- при потоковой обработке больших текстов;
- в биоинформатике для поиска фрагментов ДНК;
- как основу для построения автомата поиска одной подстроки.

## Типичные ошибки

- После нахождения вхождения поставить `k = 0` вместо `k = pi[k-1]`, потеряв перекрывающиеся ответы.
- При несовпадении двигать индекс текста назад. В КМП текст читается только вперед.
- Считать `pi` для `P#T`, а затем пытаться использовать его как `pi` только для `P`.
- Не проверять границы при `k == m`.

## Экзамен-вопросы

1. В чем отличие КМП от наивного поиска?
2. Какую роль играет префикс-функция в КМП?
3. Что означает переменная `k`?
4. Почему при несовпадении делается `k = pi[k-1]`?
5. Почему КМП работает за `O(n + m)`?
6. Как найти перекрывающиеся вхождения?
7. Почему КМП удобен для real-time обработки текста?
