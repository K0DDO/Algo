# 29. Хеширование кукушкой

## Идея

Хеширование кукушкой - метод хеш-таблиц, в котором у каждого ключа есть два возможных места.

Используются две хеш-функции:

```text
h1(x)
h2(x)
```

или две таблицы, по одной для каждой функции. В лекции указаны две вариации:

- две независимые хеш-функции;
- две таблицы для двух хеш-функций.

Хеш-функции желательно брать из универсального семейства.

## Почему "кукушка"

Кукушка подбрасывает яйцо в чужое гнездо, вытесняя другое. В алгоритме новый ключ может занять ячейку, вытолкнув старый ключ. Вытолкнутый ключ затем переселяется в свое второе возможное место.

Этот процесс называется `kick`, или вытеснение.

## Операция `contains`

Чтобы проверить наличие `x`, достаточно посмотреть только две позиции:

```text
table[h1(x)]
table[h2(x)]
```

Если в одной из них лежит `x`, ответ `true`, иначе `false`.

Сложность:

```text
O(1)
```

Это главное преимущество кукушкиного хеширования: поиск в худшем случае ограничен константным числом проверок.

## Операция `remove`

Удаление тоже простое:

1. Проверить `h1(x)` и `h2(x)`.
2. Если в одной из ячеек лежит `x`, пометить ее свободной.

Сложность:

```text
O(1)
```

В отличие от многих вариантов открытой адресации, не нужны `deleted`-метки и восстановление цепочки исследования.

## Операция `insert`

Алгоритм из лекции:

1. Если одна из ячеек `h1(x)` или `h2(x)` свободна, кладем `x` туда.
2. Иначе выбираем одну из занятых ячеек.
3. Кладем туда `x`, вытесняя старый элемент `y`.
4. Для `y` проверяем его второе возможное место.
5. Если оно свободно, кладем `y`.
6. Если занято, вытесняем следующий элемент.
7. Если возникло зацикливание, выбираем новые хеш-функции и перехешируем таблицу.

Псевдокод:

```text
insert(x):
    cur = x
    pos = h1(cur)

    repeat up to LIMIT times:
        if table[pos] is empty:
            table[pos] = cur
            return success

        swap(cur, table[pos])

        if pos == h1(cur):
            pos = h2(cur)
        else:
            pos = h1(cur)

    rehash_with_new_functions()
    insert(cur)
```

На практике ставят лимит на количество вытеснений, чтобы обнаруживать циклы.

## Что такое зацикливание

Определение из лекции:

Зацикливание - ситуация, когда при добавлении мы снова возвращаемся к ячейке, куда уже помещали элемент в этом же добавлении, и дальнейшие действия начинают повторяться.

Пример из лекции: если добавить три элемента `x, y, z`, такие что:

```text
h1(x) = h1(y) = h1(z)
h2(x) = h2(y) = h2(z)
```

то у трех элементов есть только две возможные ячейки. Разместить все три невозможно.

Ход:

1. `x` кладется в `h1(x)`.
2. `y` кладется в `h2(y)`.
3. `z` пытается занять одну из этих ячеек и вытесняет, например, `x`.
4. `x` вытесняет `y`.
5. `y` вытесняет `z`.
6. Мы вернулись к прежней ситуации.

Решение: сменить хеш-функции и перехешировать таблицу, иногда увеличить размер.

## Лекционный пример

Даны:

```text
h1(x) = x mod 8
h2(x) = floor(x / 8) mod 8
```

Вставляем:

```text
10, 22, 13, 5, 42
```

### Вставка 10, 22, 13

```text
h1(10) = 10 mod 8 = 2
h1(22) = 22 mod 8 = 6
h1(13) = 13 mod 8 = 5
```

Все первые позиции свободны, кладем:

```text
index: 0 1 2 3 4 5 6 7
value: - - 10 - - 13 22 -
```

### Вставка 5

```text
h1(5) = 5 mod 8 = 5
```

Ячейка `5` занята ключом `13`.

Считаем вторую позицию:

```text
h2(5) = floor(5 / 8) mod 8 = 0
```

Ячейка `0` свободна, кладем `5` туда:

```text
index: 0 1 2 3 4 5 6 7
value: 5 - 10 - - 13 22 -
```

### Вставка 42

```text
h1(42) = 42 mod 8 = 2
h2(42) = floor(42 / 8) mod 8 = 5
```

Обе ячейки заняты:

- в `2` лежит `10`;
- в `5` лежит `13`.

Кладем `42` в ячейку `2`, вытесняя `10`:

```text
index: 0 1 2 3 4 5 6 7
value: 5 - 42 - - 13 22 -
```

Теперь переселяем `10`. Раньше оно стояло по `h1(10) = 2`, значит пробуем `h2(10)`:

```text
h2(10) = floor(10 / 8) mod 8 = 1
```

Ячейка `1` свободна:

```text
index: 0 1 2 3 4 5 6 7
value: 5 10 42 - - 13 22 -
```

Вставка завершена.

## Сложность

`contains`:

```text
O(1)
```

`remove`:

```text
O(1)
```

`insert`:

- обычно ожидаемо `O(1)`;
- в худшем случае может попасть в цикл и потребовать `O(n)` перехеширования;
- амортизированно при хорошем выборе параметров работает быстро.

Память:

```text
O(m)
```

где `m` - размер таблицы или суммарный размер двух таблиц.

## Структуры данных

- Одна таблица с двумя функциями или две таблицы.
- Две независимые хеш-функции.
- Маркер пустой ячейки.
- Лимит вытеснений.
- Механизм перехеширования.

## Ограничения

- При высокой загрузке вероятность циклов растет.
- Требуются хорошие независимые хеш-функции.
- Вставка сложнее, чем поиск и удаление.
- Нужно уметь перехешировать таблицу.
- Если у нескольких ключей совпадают обе позиции, разместить их невозможно без смены функций или увеличения таблицы.

## Применимость

Кукушкино хеширование удобно, когда:

- нужен очень быстрый поиск с гарантированно малым числом проверок;
- удаления должны быть простыми;
- допустимы периодические дорогие перехеширования;
- можно контролировать коэффициент загрузки.

Менее удобно, когда:

- вставки должны иметь жесткую worst-case гарантию;
- хеш-функции слабые;
- таблица почти заполнена.

## Экзамен-вопросы

1. В чем идея хеширования кукушкой?
2. Почему `contains` работает за `O(1)`?
3. Как работает `kick` при вставке?
4. Что такое зацикливание?
5. Почему три элемента с одинаковыми `h1` и `h2` нельзя разместить в двух ячейках?
6. Разберите вставку `10, 22, 13, 5, 42` для `h1=x mod 8`, `h2=floor(x/8) mod 8`.
7. Почему при цикле нужно менять хеш-функции?
8. Чем кукушкино хеширование отличается от линейного исследования?
